<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ===================== Parent identity ===================== -->
    <groupId>com.cms</groupId>
    <artifactId>cms-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>cms-parent</name>
    <description>Client Management System - Multi-Module Parent</description>

    <!-- ===================== Child modules ====================== -->
    <modules>
        <module>common</module>
        <module>api-gateway</module>
        <module>client-service</module>
        <module>account-service</module>
        <module>billing-service</module>
        <module>notification-service</module>
    </modules>

    <!-- ===================== Properties ========================= -->
    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Spring -->
        <spring-boot.version>3.3.2</spring-boot.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>

        <!-- Persistence -->
        <flyway.version>10.15.2</flyway.version>
        <mysql.connector.version>8.4.0</mysql.connector.version>

        <!-- Messaging -->
        <kafka.version>3.7.1</kafka.version>

        <!-- Utilities -->
        <lombok.version>1.18.46</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <jackson.version>2.17.2</jackson.version>

        <!-- Testing -->
        <testcontainers.version>1.20.1</testcontainers.version>

        <!-- Observability (Phase 7) -->
        <!-- micrometer-tracing-bridge-otel is managed by Spring Boot BOM; version listed for reference only -->
        <opentelemetry-exporter.version>1.40.0</opentelemetry-exporter.version>

        <!-- SonarQube (Phase 9) -->
        <sonar.organization>YOUR_SONAR_ORG</sonar.organization>
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>
    </properties>

    <!-- ===================== Dependency Management ============== -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM - import first, covers most spring-* versions -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Testcontainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Internal: common module -->
            <dependency>
                <groupId>com.cms</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- OpenTelemetry OTLP exporter (Phase 7 - not in Spring Boot BOM) -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-exporter-otlp</artifactId>
                <version>${opentelemetry-exporter.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ===================== Common dependencies ================ -->
    <!-- Inherited by ALL child modules -->
    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- ===================== Build plugin management ============ -->
    <build>
        <pluginManagement>
            <plugins>
                <!-- Spring Boot plugin - only activated in service modules via <plugins> -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <!-- Compiler plugin - targets Java 21 bytecode, wires Lombok annotation processor.
                     MapStruct 1.6+ ships its own processor in the main JAR. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.14.0</version>
                    <configuration>
                        <release>21</release>
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <!-- Surefire: Java 21 requires explicit permission for Mockito 5's
                     inline mock maker to dynamically attach its byte-buddy agent.
                     Without -XX:+EnableDynamicAgentLoading, all Mockito tests fail
                     with "Could not modify class" on Java 21+. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                    <configuration>
                        <argLine>
                            -XX:+EnableDynamicAgentLoading
                            -Xshare:off
                        </argLine>
                    </configuration>
                </plugin>

                <!-- SonarQube Maven plugin (Phase 9) -->
                <plugin>
                    <groupId>org.sonarsource.scanner.maven</groupId>
                    <artifactId>sonar-maven-plugin</artifactId>
                    <version>4.0.0.4121</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!-- ===================== Profiles ========================== -->
    <profiles>
        <!-- Coverage profile: activated with -Pcoverage in CI -->
        <!-- Adds JaCoCo agent to all test runs so Sonar gets line coverage data -->
        <profile>
            <id>coverage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.8.12</version>
                        <executions>
                            <execution>
                                <id>prepare-agent</id>
                                <goals><goal>prepare-agent</goal></goals>
                            </execution>
                            <execution>
                                <id>report</id>
                                <phase>verify</phase>
                                <goals><goal>report</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
